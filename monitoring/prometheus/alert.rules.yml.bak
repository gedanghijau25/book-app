groups:

- name: book-app

  rules:

  - alert: BookBackendDown
    expr: up{job="backend"} == 0
    for: 1m
    labels:
      severity: critical

    annotations:
      summary: "Book backend is down"
      description: "Backend API tidak merespon Prometheus"
